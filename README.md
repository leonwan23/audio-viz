# A simple audio visualization project using React and D3

This project was bootstrapped with [Create React App](https://github.com/facebook/create-react-app).

## Using Docker
- Run the command `docker-compose up -d`

